app.factory('Snippet', function($http, Session, AuthService){

  var Snippet = {};

  // Snippet.update = function(snippetId, userId, updatedParams){
  //   if (AuthService.isAuthenticated()){
  //     //do something (but only if an Owner or Collaborator)
  //   }
  // }

  return Snippet;

});
